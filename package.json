{
  "name": "startup-growth-simulator-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Repo-root build tooling (e.g. presentation deck builder). The Python project lives in engine/, tests/, notebooks/, app.py. The Next.js landing page lives in landing/.",
  "scripts": {
    "build:deck": "node scripts/build_deck.js"
  },
  "dependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
